# Dependencies
node_modules/
.pnp/
.pnp.js

# Build outputs
.next/
out/
build/
dist/

# Environment variables (CRÍTICO — nunca subir)
.env
.env*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Cache
.turbo/
.vercel/

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Coverage
coverage/

# Misc
.eslintcache

# Hero background video — served raw/uncompressed at the client's request
# (~160 MB), which is over GitHub's 100 MB per-file push limit. Deployed to
# the production server as a manual step (see docs/GUIA-INFRAESTRUCTURA-IT.md)
# instead of via git. Do not remove this line without an alternative in place
# (Git LFS, or re-compressing) — committing it as-is will block every push.
public/heros/hero-main.mp4